@model VisitorSystem.Models.AdminUser
<form action="/Admin/ChangePassword" method="post">
    <div id="contents">
        <div class="sub_container">
            <div class="row">
                <div class="col md-1">
                    <div class="sub_heading border_b">
                        <h2>
                            암호 변경 <br />
                            로그인 한 아이디: @ViewBag.UserID
                        </h2>

                    </div><!-- .sub_heading(E) -->
                    <h2>비밀번호 정책에 따라 변경 1개월이 지났기 때문에 비밀번호 변경을 강제합니다.</h2>
                    @Html.ValidationSummary(true)
                    <p></p>
                    <div class="row">
                        <div class="md-1">
                            <table class="view" width="100%" summary="view">
                                <colgroup>
                                <col width="35%" />
                                <col width="35%" />
                                </colgroup>
                                <tbody>
                                    <tr>
                                        <th>변경할 암호</th>
                                        <td>@Html.PasswordFor(m => m.AdminNewPW)</td>
                                    </tr>
                                    <tr>
                                        <th>암호 재확인</th>
                                        <td>@Html.PasswordFor(m => m.AdminNewRePW)</td>
                                    </tr>
                                    <tr>
                                        <th></th>
                                        <td><span style="color:red;">@ViewBag.Error</span></td>
                                    </tr>
                                </tbody>
                            </table>
                            <div class="btn_area view">
                                <button class="btn mr10" type="submit">저장</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>